export const environment = {
  production: true,
  apiBaseUrl: 'https://api.ukstore.com/api',
  apiVersion: 'v1',
  logLevel: 'error',
  cacheExpiry: 10 * 60 * 1000,
  requestTimeout: 45000,
  retryAttempts: 3,
  retryDelay: 2000
};
